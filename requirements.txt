# LocalBib core - Dependencies
# Plugin-only dependencies live in requirements-plugins.txt and are
# deliberately NOT listed here: the public export ships the core tree only
# (issue #143), so `pip install -r requirements.txt` must be enough to run it.
PyMuPDF>=1.24.0       # PDF-Verarbeitung + OCR (mit Tesseract)
watchdog>=4.0.0        # Ordner-Überwachung
requests>=2.31.0       # HTTP (CrossRef API, LLM-Provider)
httpx>=0.27.0          # HTTP (OpenAlex API)
certifi>=2024.2.2      # Wurzelzertifikate; ca_trust führt sie mit einem Haus-Bundle zusammen
rapidfuzz>=3.6.0       # Fuzzy String Matching (Referenz-Duplikaterkennung)
bibtexparser>=1.4,<2   # BibTeX-Import (.bib parsen; v2 ist ein inkompatibler Rewrite)

# Web Application
fastapi>=0.115.0,<0.116  # REST-API Backend (Starlette >=1.x bricht TemplateResponse-Altform -> 500)
uvicorn[standard]>=0.32.0  # ASGI Server
python-dotenv>=1.0.0   # .env Datei laden
jinja2>=3.1.0          # HTML Templates
python-multipart>=0.0.6  # File-Uploads (Citavi/RIS Import, Icon Upload)

# Optional (Windows-Verknüpfungen)
# pywin32>=306          # Nur auf Windows, für .lnk Shortcuts
