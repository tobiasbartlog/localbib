{
  "name": "literature-manager-v3",
  "version": "0.0.0",
  "private": true,
  "description": "JS smoke tests for the buildless Vue-3-CDN SPA (static/app.js). The app itself stays buildless; npm is test-only.",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "dompurify": "3.4.14",
    "jsdom": "^26.1.0",
    "marked": "12.0.2",
    "vitest": "^3.2.4",
    "vue": "^3.5.13",
    "vue-router": "^4.5.0"
  }
}
